Title: Cover MXFP8/NVFP4 quantization in distributed regression suite

Summary:
- extend tests/pytorch/distributed/run_cast_master_weights_to_fp8.py to accept
  MXFP8BlockScaling and NVFP4BlockScaling recipes, expose raw uint8 buffers for
  MXFP8/NVFP4 tensors, and allow CLI selection of the new modes
- gate pytest launcher on is_mxfp8_available/is_nvfp4_available so the new
  modes are scheduled only when supported (and skip otherwise)
- refresh llm.state and quants-dev.md to record the expanded test coverage

Testing:
- source venv/bin/activate
- MAX_JOBS=30 NVTE_BUILD_THREADS_PER_JOB=1 NVTE_FRAMEWORK=pytorch \
  python -m pytest tests/pytorch/distributed/test_cast_master_weights_to_fp8.py \
  -k 'nvfp4 or mxfp8' --maxfail=1
