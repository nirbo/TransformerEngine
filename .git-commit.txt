Changes:
- Added QuantizationConfig.block_size validation to NVFP4 quantize and transpose entry points so host code now rejects unsupported blocks.
- Updated llm.state to reflect the broader validation coverage and remaining kernel work.

Testing:
- Not run (logic adds host-side assertions only).
